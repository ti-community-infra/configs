presubmits:
  tikv/pd:
    - name: tikv/pd/ghpr_build
      agent: jenkins
      decorate: false # need add this.
      always_run: false
      context: wip-idc-jenkins-ci/build
      trigger: "(?m)^/test (?:.*? )?build(?: .*?)?$"
      rerun_command: "/test build"
      branches:
        - ^master$
        - ^release-6.[5-9](\.\d+(-\d+)?)?$
        - ^feature[_/].+